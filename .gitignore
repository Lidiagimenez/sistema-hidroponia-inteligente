.venv/
__pycache__/
*.pyc
.env

# Base de datos local (SQLite) - NO se versiona el binario.
# Solo puede existir en /data, así que lo apuntamos explícito ahí.
/data/db.sqlite3
/db.sqlite3

# Pero SÍ versionamos la carpeta para que exista tras el clone
!data/.gitkeep